[project]
name = "dm-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "redisnotbluedev", email = "red@redisnot.blue" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
dm-analysis = "dm_analysis:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
